<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:gravity="center">

    <!-- App Logo -->
    <ImageView
        android:id="@+id/appLogo"
        android:layout_width="372dp"
        android:layout_height="180dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:src="@drawable/main_logo" />


    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="30dp"
        app:cardCornerRadius="30dp"
        app:cardElevation="20dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="480dp"
            android:layout_gravity="center_horizontal"
            android:background="@drawable/border_color"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- Username EditText (initially hidden) -->
            <TextView
                android:id="@+id/loginTextView"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:text="Login"
                android:textSize="35sp"
                android:textAlignment="center"
                android:textColor="@color/Lightgrey" />


            <EditText
                android:id="@+id/WardsLoginUsername"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/border_color"
                android:drawableLeft="@drawable/baseline_person_24"
                android:hint="Username"
                android:padding="8dp"
                android:textColor="@color/black" />


            <!-- Password EditText (initially hidden) -->
            <EditText
                android:id="@+id/wardsLoginPassword"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/border_color"
                android:hint="Password"
                android:padding="8dp"
                android:textColor="@color/black" />
            <!-- Register Button -->
            <Button
                android:id="@+id/wardsLoginButton"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_marginTop="10dp"
                android:text="Login" />

            <!-- Login Button -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/wardsRegisterRedirect"
                android:text="Not yet registerd? register"
                android:layout_gravity="center"
                android:padding="8dp"
                android:layout_marginTop="10dp"
                android:textSize="18sp"
                android:textColor="@color/orange"/>

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</LinearLayout>
